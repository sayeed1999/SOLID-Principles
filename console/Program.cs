using System;
using OOP;
using static System.Console;

namespace console
{
    class Program
    {
        static void Main(string[] args)
        {
            // Teacher p = new Teacher { Name = "Mantu Ram", Department = DepartmentEnum.Math, Designation = TeacherDesignationEnum.AssistantLecturer };
            // WriteLine(p.ID);
            // Person p1 = new Student();
        }
    }
}
